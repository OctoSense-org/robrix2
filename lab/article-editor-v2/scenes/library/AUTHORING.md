# Robrix native adapter

See `robrix-mapping.json` for reviewed roles and actual native widget IDs. Source pixels and inverse crop transforms are preserved by intake/prepare. The runtime target is the Robrix host, with an Octoscript L0 title/source binding and a Makepad rich-input implementation; the upstream generic kit compiler is not claimed as executed. Native evidence is tracked separately in the v2 validation report.
